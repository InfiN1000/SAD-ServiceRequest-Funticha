# SAD-ServiceRequest Prototype

Simple ICT Service Request management prototype using Supabase.

Run: open `login.html` and `index.html` in browser (deployed via GitHub Pages).

Supabase project URL and publishable key are configured in `js/supabase.js`.
"# AD-ServiceRequest-Funticha" 
